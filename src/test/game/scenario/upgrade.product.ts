import {chai, assert, Storage} from "../../test.commons"
import {Managerist} from "../../../app";
import {AlertType} from "../../../models/alerts/alert.type";
import {Alert} from "../../../models/alerts/alert";
import {EventType} from "../../../models/event.type";
import {Event} from "../../../models/event";
import {TestGame} from "../utils/test.game"
import { Project } from "../../../models";

describe('Production, perform product upgrade  test', () => {
    xit('Schedule product upgrade (new project).', (done:Function) => {
        //TODO
    });
    xit('Burnout upgrade project test.', (done:Function) => {
        //TODO
    });
    xit('Check Marketings/Finance metrics increase for Product after upgrade.', (done:Function) => {
        //TODO
    });
});
